/*
* Export icons that are used for components and are not imported from a library like tabler
* The .svg files need to be in the Icons folder
*/
const internalIcons = {
    collapseLeft: require('../Icons/collapseLeft.svg').default,
    collapseRight: require('../Icons/collapseRight.svg').default,
    settingsOpen: require('../Icons/settingsOpen.svg').default,
  };

export default internalIcons;